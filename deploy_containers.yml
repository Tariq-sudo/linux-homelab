---

- name: Pull and Run Nginx Container
  community.docker.docker_container:
    name: nginx-web
    image: "{{ docker_image }}"
    state: started
    restart_policy: always
    ports:
      - "80:80"
